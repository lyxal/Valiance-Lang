define fizzbuzzFor(x: Number) -> String {
    $res = ""
    if ($x 3 divides?) {$res: {"Fizz" +}}
    if ($x 5 divides?) {$res: {"Buzz" +}}
    if ($res "" ==) {$res = {$x toString}}
    $res
}

range(1, 100) map: fizzbuzzFor
foreach (s) {println(s)}