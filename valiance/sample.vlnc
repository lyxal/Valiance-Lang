tag computed #sorted {
  (take, drop)[T]: (#sorted T+, Number) -> #sorted T+
}

tag extend #sorted {
  filter[T]: {
    (#sorted T+, Function[T -> #boolean Number]) -> #sorted T+,
    (#sorted T+, Function[T -> Number]) -> #sorted T+
  }
}