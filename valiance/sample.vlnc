import std
std.readLine parseInt match {
  as :Number -> {
    match {
      exactly 0 -> "Zero!",
      exactly 1 -> "One!",
      if inrange(2, 10) -> "Between two and ten!",
      _ -> "What kind of number is that?"
    }
  },
  as :None   -> "Not a number!",
}

println