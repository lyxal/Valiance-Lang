PROGRAM → (VARIABLE_ASSIGN | EXPRESSIONABLE)*
VARIABLE_ASSIGN → "~:" (ASSIGN_MEMBER | ASSIGN_NORMAL)
ASSIGN_MEMBER → "." NAME
ASSIGN_NORMAL → NAME
EXPRESSIONABLE → (VARIABLE_GET | LITERAL | KEYWORD | TUPLE | FUNCTION_CALL)*
VARIABLE_GET → "$" (GET_MEMBER | GET_NORMAL)
GET_MEMBER → "." NAME
GET_NORMAL → NAME
LITERAL → STRING | NUMBER | LIST | FUNCTION
NUMBER → DECIMAL ("i" DECIMAL)?
DECIMAL → DIGITS ("." DIGITS)? "_"?
DIGITS → 0 | ([1-9][0-9]*)
STRING → '"' ([^"\] | '\' .)* '"'
LIST → "[" (EXPRESSIONABLE ("," EXPRESSIONABLE)*)? "]"
FUNCTION → "{" 
              ( "(" FUNCTION_PARAMETERS? ")" 
                 ("->" TYPE ("," TYPE)*)? 
                 "=>"
              )?
            PROGRAM "}"
FUNCTION_PARAMETERS → FUNCTION_PARAMETER ("," FUNCTION_PARAMETER)*
FUNCTION_PARAMETER → DIGITS | REPETITION | (NAME (":" TYPE)?) | (":" TYPE REPETITION?) | NAMED_TUPLE
REPETITION → "{" DIGITS | REPTITION_EXPRESSION "}"
REPETITION_EXPRESSION → (DIGITS | NAME) | (REPETITION_EXPRESSION ("+" | "-" | "*" | "^") (DIGITS | NAME))
NAMED_TUPLE → "@(" (NAME ":" TYPE) ("," NAME ":" TYPE)* ")"
TYPE → (NAME | BUILT_IN_TYPE) GENERICS? TYPE_MODIFIERS?
TYPE_LIST → TYPE ("," TYPE)*
GENERICS → "[" TYPE_LIST "]"
TYPE_MODIFIERS → ("?" | "!" | "~" | "+")*
BUILT_IN_TYPE → _NUMBER_TYPE | _STRING_TYPE | _FUNCTION_TYPE | _UNIT_FUNCTION_TYPE | _ARITY_DEPENDENT_FUNCTION_TYPE
_UNIT_FUNCTION_TYPE → "(Unit" | "⨚") GENERICS?
_ARITY_DEPENDENT_FUNCTION_TYPE → ("ADFunction" | "𝕗") FUNCTION_TYPE_GENERICS?
_NUMBER_TYPE → "Number" | "ℕ"
_STRING_TYPE → "String" | "𝕊"
_FUNCTION_TYPE → ("Function" | "𝔽") FUNCTION_TYPE_GENERICS?
FUNCTION_TYPE_GENERICS → "[" TYPE_LIST (";" TYPE_LIST)* "]"
NAME → [a-zA-Z_][a-zA-Z0-9_]*
KEYWORD → [a-zA-Z=+-/*^\<>!%&_]+
TUPLE → "@(" (EXPRESSIONABLE ("," EXPRESSIONABLE)*)? ")"
FUNCTION_CALL → "`" NAME "`"