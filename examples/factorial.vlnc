## Approach 1 - The Obvious Way

iota product
## OR
iota fold: *
## OR
1;range product

## Approach 2 - Recursion

{
    #match: {
        0 => 1 |
        => thisFN(arg--) *
    }
}

## Approach 3 - Iteration

{
    @(1, arg)
    while: {
        @(acc: Number, n: Number) =>
        $n non-zero? |
        @(acc * n, n - 1)
    }
    head
}
