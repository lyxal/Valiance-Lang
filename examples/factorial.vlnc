## Approach 1 - The Obvious Way

iota product
## OR
iota fold: *
## OR
1 swap range product

## Approach 2 - Recursion

{(n: Number) -> (Number) =>
  thisFN ::= factorial
  switch: {$n 0 ==} { 1 } {
    $n 1 - $factorial() $n *
  }
}